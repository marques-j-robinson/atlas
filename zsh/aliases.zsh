alias szsh="source ~/.zshrc"

# Kafkacat
alias kafkacat="kafkacat -b 10.0.101.172:9092 -X broker.version.fallback=0.8.2.0  -X api.version.request=false"

# Python
alias python="python3"
alias pip="pip3"

# Docker
alias updog='docker-compose up -d'